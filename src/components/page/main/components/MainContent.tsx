import { Box } from '@chakra-ui/react';
import { FC } from 'react';

import { MainPageProps } from '../hooks/useMainPageHook';

export const MainContent: FC<MainPageProps> = () => {
  return <Box>マインページ</Box>;
};
